// import { useEffect } from 'react';

// const useLoginEffect = (setLoggedIn) => {
//   useEffect(() => {
//     //셰션스토리에서 로그인 키에 저장된 값 가져온다
//     const storedLoggedIn = sessionStorage.getItem('loggedIn');
//     // LoggedIn 값이 존재하는 경우
//     if (storedLoggedIn) {
//       // 로그인 상태를 true로 설정
//       setLoggedIn(true);
//     }
//   }, [setLoggedIn]);
// };

// export default useLoginEffect;